# Claude skill working directories
.claude-work/

# Local editor history
.history/

# Machine-local Claude Code state. These files belong to the machine and are
# never managed or committed by this repo. `~/.claude.json` holds user-scope
# MCP servers; `mcp.json` stays as a defensive entry.
settings.json
mcp.json
.claude.json

# Machine-local and environment state
*.local.*
.env
.env.*

# Private keys
*.pem
*.key
*.ppk
id_rsa
id_ed25519

# OS junk
.DS_Store
