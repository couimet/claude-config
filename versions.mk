# Package versions, pinned to match the shared actions (couimet/github-actions).
# Bump here when the shared actions advance, so local runs mirror CI.
# This file holds npm package versions only. The dev tools (node, bats, and
# shellcheck) are pinned in mise.toml, which `make install-prereqs` installs.
MARKDOWNLINT_VERSION := 0.23.2
